<section>
  <p><b>displayName</b> {{user.displayName}} </p>
  <p><b>email</b> {{user.email}} </p>
  <p><b>photo</b> <img src="{{user.photo}}" alt="Foto de {{user.displayName}}"> </p>
  <p><b>registered</b> <time>{{ user.registered }}</time> </p>
  <p><b>lastConnection</b> <time>{{ user.lastConnection }}</time> </p>
</section>